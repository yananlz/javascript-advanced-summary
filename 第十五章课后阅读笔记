Math属性：Math.E：欧拉常数(e);Math.LN2:2的自然对数;Math.ln10:10的自然对数;Math.log2E:以2为底的e的对数;Math.PI:圆周率（3.14159……），Π;Math.SQRT1_2:1/2的平方根;Math.SQRT2:2的平方根。
数值函数：Math.abs(x):返回x的绝对值。Math.ceil(x):返回大于等于x的最小整数。Math.exp(x):返回e(Math.E)的X次幂。它是Math.log()的逆运算。Math.floor(x):返回小于等于x的最大整数。
Math.log(x):返回x的自然对数，是Math.exp()的逆运算。Math.pow(x,y):返回x的y次幂。Math.round(x):返回最接近x的整数(如果这个数字恰好等于靠近它的两个数字的平均数，那么就选择较大的那个)。
Math.sqrt(x):返回根号x。
三角函数：Math.acos(x):返回x的反余弦值。Math.asin(x):返回x的反正弦值。Math.atan(x):返回x的反正弦值。Math.atan2(y,x):返回y/x商的反正切值。Math.cos(x):返回x的余弦值。
Math.sin(x):返回x的正弦值。Math.tan(x):返回x的正切值。
其他函数：min(x1,x2,……):返回参数中最小的数字。max(x1,x2,……):返回参数中最大的数字。Math.random():返回一个伪随机数r,0<=r<=1。
JSON将数据存储为纯文本格式。
例：{
      "first":“Jane”,
      "last":"Porter",
      "married":true,
      "born":1890,
      "firends":['Tarzan','Cheeta']
    }。
JSON使用JavaScript表达式的如下结构：(1):复合：JSON数据的对象和数组。(2):原子：字符串、数值、布尔值和null。
JSON原则：字符串必须使用双引号，属性键必须使用双引号。
JSON.stringify(value,replacer,space):将value转换为JSON字符串。可选参数：replacer：用于转换前替代参数value。
可选参数space会影响输出格式。如果没有这个参数，字符串转换后的结果将会是单行的文本。
replacer:节点访问函数，会在值被转换为字符串之前转换树节点的值。属性键白名单，用于隐藏那些非数组对象内属性不在这个列表中的所有属性。
space：可以插入新行并且通过数组和对象的嵌套增加缩进。
如果JSON.stringfy()遇到一个对象，它具有toJSON方法，那么可以使用该方法来获得字符串化的值。
JSON.parse(text,reviver):解析文本格式的JSON数据，然后返回相应的值。reviver参数可选，它是一个节点访问函数，可以用来转换解析后的数据。
JSON.stringfy():可以在转换为JSON前改变数据。JSON.parse():解析JSON，并可以后处理生成的结果数据。
JSON.stringify():采用的是前序遍历算法，首先访问的是那特殊的根节点，也称伪根节点。最后一行的结果是所有遍历之后，stringfy()返回的字符串。
JSON.parse()：采用的是后序遍历算法，叶节点先被访问，而伪根节点最后被访问，最后一行的结果是所有遍历知后，parse()返回的值。
